import { Controller } from "@nestjs/common";

@Controller("auth")
export class AuthController {
  // Auth routes will be handled by Clerk
  // This controller can contain additional auth-related endpoints
}
